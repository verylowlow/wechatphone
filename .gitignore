# Python
__pycache__/
*.pyc

# Knowledge base data (uploaded docs + sqlite index)
data/

# Temp
*.tmp

# WorkBuddy local memory (not part of the project)
.workbuddy/

# NOTE: .env is currently tracked (user's v1.0.0 baseline includes it).
# If you want to stop tracking it: git rm --cached .env
